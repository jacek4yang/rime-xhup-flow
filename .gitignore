# Rime 本地部署生成文件
/build/
*.bin
*.reverse.bin
*.prism.bin
*.table.bin

# 设备身份与本机状态
/installation.yaml
/user.yaml
/sync/

# 个人用户词典 / LevelDB
*.userdb/
*.userdb.txt
*.ldb
LOCK
LOG
LOG.old
MANIFEST-*
CURRENT

# 本地备份与压缩包
*.7z
*.zip
*.tar
*.tar.gz
*.bak
*~

# 系统与编辑器文件
.DS_Store
Thumbs.db
.vscode/
.idea/

# Rust 构建产物
/target/

# XHUP Flow 本地生成产物
/dist/

# AI 工具本地会话与状态(仅明确属于本地数据者;工具的项目级配置目录不整体忽略)
.aider.chat.history.md
.aider.input.history
.aider.llm.history
.aider.tags.cache.*
.claude/settings.local.json

# Node/Vite/TypeScript 本地产物(根目录测试缓存等)
node_modules/
*.tsbuildinfo

# 小程序本地产物(dist 由 taro 构建;分片由脚本从规范数据确定性生成)
miniapp/dist/
miniapp/src/data/generated/
miniapp/.swc/

# Python 脚本本地缓存(kdconv 语料脚本相互导入产生)
__pycache__/
